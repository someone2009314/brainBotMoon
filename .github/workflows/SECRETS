name: SECRETS

on:
  push:
    branches:
      - main

jobs:
  example_job:
    runs-on: ubuntu-latest

    steps:
      - name: Example Step
        run: |
          echo "My secret value is ${{ secrets.TEST_1}}"
